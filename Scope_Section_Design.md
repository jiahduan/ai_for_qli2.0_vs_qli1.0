# 设计：给对比文档框架新增《对比范围》环节

日期：2026-08-27
状态：待确认（确认后按此顺序实施：先改Methodology.md模板 → 小范围试点2-3篇验证模板 → 试点通过后按复杂度分层对剩余30篇做核实回填）

## 背景

当前Methodology.md规定的分析工作流第2步是"明确本主题对应的双侧目录/文件范围"，但这一步是内部心算，不落笔、不进最终文档。结果是：读者看到的每篇主题文档，只有取证之后的结论（对比总览/关键差异/影响与风险），看不到"这篇文档故意管了什么、故意没管什么、为什么"。

这带来两个具体问题：
1. 分类之间存在天然交叉的主题（如Bootloader的secure boot签名同时牵涉Boot_Architecture与安全，Toolchain同时牵涉Build_Architecture与Code_Composition）时，排除的一侧没有留痕，读者无法确认"没提到"是遗漏还是故意归给了别的文档。
2. 深挖程度不一致——同类文档有的几十行、有的几百行——部分原因是没有一个前置的范围声明来框定"这次到底该深挖到哪儿"，导致深挖边界随写作过程漂移。

本次改动只解决"执行流程/产出模板缺先定范围这一步"，不涉及：
- 顶层5大分类（System/Boot/Build/Code_Composition/Platform_Features）本身的划分是否合理
- 已有《关键差异》《影响与风险》结论的重新评估（除非核实范围时发现原结论确实有误，按Methodology规则4走纠错记录）

## 目标

1. 让"定范围"从隐性动作变成显式、可审计、必须落笔的交付物，且必须在取证之前完成。
2. 让"明确排除"的项能相互对得上号——A文档说某项归B文档，B文档必须真的覆盖了它，不能出现悬空指向。
3. 33篇现有文档全部回填，按复杂度/纠错历史分层核实（复杂或有纠错历史的深度重新核实，稳定简单的先核对现有证据链，不够再升级），不是无差别地对所有文档用同一套重量级流程，也不是照抄现有正文改写。

## 设计

### 1. Methodology.md 规则2（统一文档结构）新增强制第0节

在`## 对比总览`之前插入：

```markdown
## 对比范围

- **覆盖**：<本文实际比较的子项列表，每项标明双侧目录/文件锚点；单条bullet过长(经验值:超过约150字)时拆成更细的子bullet，不要塞成一段长文字>
- **明确排除**：<本主题名义上涉及、但结论归属别的文档的项——不要求"正文已经字面提到过才能排除"，只要读者可能预期在本文找、实际该去别的文档找，就该排除>
  - <项1> ——见[YY](相对路径/YY.md)
  - <项2> ——见[ZZ](相对路径/ZZ.md)
- **待定边界**：<暂时定不下来该归哪篇、先记录别漏掉的项；随本文档下次修订时顺带复核，不单开复核周期；若长期悬而未决，同步进README《待拍板事项汇总》>
```

多条"明确排除"时每条单独一行（子bullet），只写"项名 ——见[文档](路径)"，不展开排除理由（理由留给目标文档自己交代，或在正文引用处简要说明，不塞进这一行）——不要用分号堆在同一行，方便脚本按行解析、也方便人工审阅。

三个字段都必须存在；"明确排除""待定边界"若确实为空，写"（无）"；如果是"核实过确认没有"而非"没检查"，可以写成"（无，已核实XX）"简要说明核实范围，不算违反"为空写（无）"的要求。

### 1.1 试点后模板修订（2026-08-27，基于Branch_Management/WiFi_BT/Security_Architecture三篇试点反馈）

试点执行后发现原模板存在4处问题，已采纳并同步更新到本文档与全部33份`rules/<分类>/<主题>.md`（本次改动涉及33处同步，是"34份完整独立、不共享基础"这一设计选择的直接代价，试点验证了该代价是可管理的）：

1. **"明确排除"定义歧义**：Security_Architecture试点发现严格按"正文已讨论过"理解会漏掉"主题上归属但正文从未提及"的真实排除项（如dm-verity/AVB等安全话题从未在Security_Architecture.md正文出现，但读者会预期在这里找）。已放宽定义,不要求字面提及。
2. **单行过长**：WiFi_BT试点的覆盖项单条bullet塞进"锚点+说明"接近400字，失去列表可扫读性。已加入约150字的经验性拆分提示。
3. **"(无)"与核实说明的写法冲突**：模板字面要求空字段写"(无)"，但"已核实确认没有"和"没检查"需要区分。已明确允许"(无,已核实XX)"形式。
4. **排除理由拖长排除行**：已明确排除项只写"项名+去向"，理由留给目标文档或正文引用处，不占用排除行本身。

试点还验证了：全局脚本`check_scope_links.sh`及3个对应的薄包装脚本在真实内容下运行正常（不会对"目标文档尚未回填"误报悬空引用，只对"目标文件本身不存在"报错，符合渐进式回填的实际需要）；耗时上，"轻量核对"型主题(如Branch_Management)约10次检索、"深度核实"型主题(如WiFi_BT、Security_Architecture)约20-30次检索+15-20分钟,大致印证了分层执行的必要性。

**与《对比总览》的分工**：《对比范围》是文字化的元信息（管辖边界、目录锚点、排除去向），《对比总览》是纯事实对比表（维度|downstream(maili)|QLI2.0）。两节不互相转述——《对比范围》不重复总览表已有的对比结论，《对比总览》也不解释为什么某项不在表里。

### 2. Methodology.md 分析工作流第2步改写

原文："明确本主题对应的双侧目录/文件范围（参考下方《分类专属取证指引》定位起点）"

改为："确认范围并写出《对比范围》三个字段——覆盖项要有双侧目录锚点佐证，排除项要点名指向具体承接文档，此步产出直接写入文档正文，不是内部心算。完成本步之后才能进入下一步取证；若取证过程中发现实际边界与本步写下的不一致，回来更新本节，不静默扩大或收窄范围。"

### 3. 跨文档一致性规则（Methodology.md 新增一条校验规则）

新增到"强制规则"部分（建议编号为规则7，紧跟现有规则6"交叉验证"之后）：

> ### 7. 范围交叉一致性
>
> 任何文档《对比范围》里的"明确排除"，必须能在被指向的文档的"覆盖"字段里找到对应项；反之，任何"待定边界"项，不得同时被两篇文档都排除或都不提及。写完/改完一篇文档的《对比范围》后，必须检查它指向或被指向的文档是否需要同步更新。

与其他强制规则同等级别，不降级为建议做法。

配一个轻量校验脚本（如`scripts/check_scope_links.sh`或等价的一行`grep`组合）：抽取每篇《对比范围》"明确排除"里指向的文档名，检查该文档确实存在，并检查其"覆盖"字段里能找到对应的项目关键字。脚本只做字符串级别的粗检查（抓悬空引用/明显缺失），不能替代人工判断措辞是否准确，但能防止规则7变成纯人工执行、无人验证的僵尸规则。脚本本身作为本次实施的一部分产出，不是事后可选项。

### 4. 试点验证（推广到全部33篇之前）

模板（第0节+工作流+规则7）改完后，先选3篇代表性文档验证格式是否够用，不直接推全部：
- 1篇简单稳定的（如`Branch_Management.md`，验证轻量场景不会小题大做）
- 1篇复杂多子方向的（如`WiFi_BT.md`，验证"覆盖"字段能否装得下多个子项而不失可读性）
- 1篇历史上被多次纠错的（如`Security_Architecture.md`，验证"待定边界"能否真实捕捉到那些反复被推翻的判断）

试点后检查：三字段格式是否需要调整（如"覆盖"要不要允许嵌套子项、是否需要额外锚点格式）、写一篇实际耗时多久（用于估算剩余30篇的工作量）、规则7脚本能否正确跑通这3篇。试点结果如导致模板调整，先更新Methodology.md，再继续推剩余30篇；试点通过则直接沿用当前模板。

### 5. 剩余30篇存量文档回填的执行方式（按复杂度分层，非一视同仁）

（本次spec范围内只定规则，实际回填在试点通过后才开始，作为后续实施计划的内容，此处先定执行标准，供实施阶段直接遵守）

分层标准：
- **深度重新核实**（回源码树重新走一遍该主题的《分类专属取证指引》）：适用于git历史上出现过纠错记录的主题（对照README《曾纠正过的结论》与各文档内部纠错记录）、或内容体量明显大于同类平均水平的主题（如Kernel_Code_Architecture、Yocto、Overlay等）
- **轻量核对**（先检查文档现有证据链——已引用的文件路径/命令输出/commit hash——能否直接支撑范围声明；能支撑则据此写范围，不重新跑取证命令）：适用于内容稳定、无纠错历史的简单主题；核对中如发现现有证据链不足以支撑范围声明（比如证据点太少、或明显有未覆盖的相关目录未被提及），当场升级为深度重新核实，不勉强凑数

- 核实/核对中如发现现有正文"其实讨论到了但没写进覆盖范围"或"标题暗示了但正文没真正涉及"的偏差，一并记录，不隐藏。
- 全部33篇（含试点3篇）的"明确排除"/"待定边界"填完后，跑一次规则7脚本做全量交叉检查，修正悬空引用。
- 产生的新"待定边界"或需要决策的归属问题，同步进README《待拍板事项汇总》。
- 若核实边界过程中推翻了原有结论，按Methodology规则4记录纠错，重要的同步README《曾纠正过的结论》。

## 验收标准

- Methodology.md 有新的规则2第0节模板、改写后的工作流第2步、新规则7。
- 规则7配套的校验脚本存在且可运行，能正确识别悬空的"明确排除"指向。
- 3篇试点文档验证通过（或按试点反馈调整模板后再通过）。
- 33篇主题文档每篇都有《对比范围》节，三字段齐全（允许"无"）；分层执行记录清楚哪些走了深度核实、哪些走了轻量核对及升级原因。
- 全量交叉检查（脚本+人工）后，不存在悬空的"明确排除"指向。
- 因回填产生的新待拍板项/纠错记录已同步进README对应汇总表。

## 范围之外（本次不做）

- 不重新划分顶层5大分类边界。
- 不重新评估已有《关键差异》《影响与风险》结论，除非核实范围时被动发现确有错误。
- 不改变Methodology.md其余强制规则（证据标准、抽样声明、纠错记录、风险分级）。
- 不建立跨33篇的全局范围地图/索引（如README层面的覆盖总览）——这是分布式声明方式的已知局限，本次先不解决，留作后续按需决策。

## 执行顺序（用户明确要求+架构复核后调整）

1. 本spec确认。
2. 修改Methodology.md：新增第0节模板、改写工作流第2步、新增规则7，并配套写规则7的校验脚本。
3. 小范围试点：3篇代表性文档（简单/复杂/纠错历史）验证模板与脚本，如需调整模板先回到步骤2。
4. 试点通过后，按复杂度分层对剩余30篇执行核实回填（深度核实 vs 轻量核对，视情况升级）。
5. 全量（33篇）跨文档一致性检查（脚本+人工）与README同步作为收尾步骤。
