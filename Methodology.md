# downstream(maili) vs QLI2.0 源码差异分析方法论

本文档规定本仓库所有主题文档必须遵循的分析方法与文档规范,目的是让新增/深化任何主题时,结论质量不随文档数量增多而衰减。本文档只讲全局性的"怎么分析、怎么写"(工作流、与README汇总表的关系);7条强制规则全文与每个主题专属的取证指引,已下沉到`rules/<分类>/<主题>.md`,与产出文档一一对应(如`rules/Build_Architecture/Toolchain.md`对应`output/Build_Architecture/Toolchain/Toolchain.md`)——重构背景见[Scope_Section_Design.md](Scope_Section_Design.md)。具体结论见各主题文档及README.md。

## 规则文件索引

7条强制规则(证据引用标准/统一文档结构/抽样声明/纠错记录/风险分级/交叉验证/范围交叉一致性)与5大分类专属取证指引,统一放在`rules/`目录下,按`rules/<分类>/<主题>.md`与产出文档同名一一对应,不在本文件重复(33篇主题各一份完整独立,变更需同步维护,这是本次目录重构明确选择的方式)。新增/修改任何主题文档前,先读该主题对应的规则文件。跨文档一致性校验脚本见`scripts/`(全局脚本+每主题一份薄包装),外部参考资料存档见`reference/`,均按同一套`<分类>/<主题>`结构与产出文档镜像。

## 分析工作流(建议顺序)

1. 确认双侧源码根路径可访问(见README.md开头)
2. 确认范围并写出《对比范围》三个字段——覆盖项要有双侧目录锚点佐证,排除项要点名指向具体承接文档,此步产出直接写入文档正文,不是内部心算。完成本步之后才能进入下一步取证;若取证过程中发现实际边界与本步写下的不一致,回来更新本节,不静默扩大或收窄范围。
3. 提取事实证据(版本号、配置项、patch/commit统计等),逐条标注来源
4. 交叉检查:对不确定项换一个角度二次验证(如版本号同时看配置文件字面值和`git log`/`git describe`),或按规则6查阅官方资料
5. 若涉及规模化内容,做抽样验证并按规则3声明方法与局限
6. 撰写《关键差异》:综合归纳,不罗列表格差异
7. 撰写《影响与风险》:识别下游需要决策/关注的具体事项,按规则5分级
8. 若结论纠正了先前判断,按规则4记录;若发现的风险/待决策事项足够重要,同步更新README.md的汇总表格(《专有栈→开源栈对照表》《安全/合规风险清单》《待拍板事项汇总》《曾纠正过的结论》,视内容对应到具体表)

## 与README.md汇总表的关系

主题文档记录的是"这一个主题内部"的完整分析过程与证据;README.md的几张汇总表(《专有栈→开源栈对照表》《安全/合规风险清单》《待拍板事项汇总》《曾纠正过的结论》)是跨主题的高层索引,只收录足够重要、需要决策层或其他团队直接看到的条目。写完一个主题文档后,应主动判断其中是否有条目需要镜像进README汇总表,而不是假设README会自动同步。
